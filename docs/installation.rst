============
Installation
============

At the command line::

    $ easy_install hypnobox-django

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv hypnobox-django
    $ pip install hypnobox-django
